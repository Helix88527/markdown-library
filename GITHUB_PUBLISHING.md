# 发布源码包

仓库建议名称：`markdown-library`。发布内容是技能规则、脚本、模板、测试与示例配置。

1. 运行 `python -m unittest discover -s tests -q`。
2. 运行 `python scripts/export_github_package.py <新的导出目录>`，检查 `EXPORT_MANIFEST.json` 与文件清单。
3. 确认示例配置没有替换为个人配置；不加入真实素材、转写稿、模型、环境、状态、私有路径、凭据或未经授权的图片。
4. 在用户指定的平台创建仓库并上传导出内容；远端创建和推送依照用户的发布要求执行。

现有 LICENSE 保留全部权利。公开可见并不代表允许他人使用、修改或再分发；若希望作为开源项目供他人复用，应由权利人选定许可后替换 LICENSE。

白名单导出会生成逐文件 SHA256 清单并检查常见凭据和个人目录路径；它不能代替对发布内容的审阅。不要上传整个已安装的技能目录或其 Git 历史。
